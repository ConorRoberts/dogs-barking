describe("Test /api/school", () => {
  const schoolId = "";
  
  // TODO
  it("List of all schools should only contain school type", () => {
    expect(true).to.be.true;
  });

  describe("Test /api/school/{id}", () => {
    // TODO
    it("Should have status 400 with an invalid ID", () => {
      expect(true).to.be.true;
    });
  });
});

export {};
