/* eslint-disable no-unused-vars */
enum validCommands {
    QUERY = "query",
    GRAPH = "makegraph",
    FLAGS = "hdrw",
    ASCENDING = "asc",
    DESCENDING = "dsc",
}

export default validCommands;
