name: "Lint"
on: [pull_request]
jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: npm i
      - name: Run ESlint
        run: npm run lint
      - name: Run Prettier
        run: npm run format
